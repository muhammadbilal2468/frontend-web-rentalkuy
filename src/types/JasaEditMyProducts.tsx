export interface JasaEditMyProductRequest {
  name: string;
  price: string;
  image: string;
  description: string;
  quantity: string;
}

export interface JasaEditMyProductResponse {
  msg: string;
}
